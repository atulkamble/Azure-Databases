# Azure Cache for Redis Sample

Python example using `redis` client. Set REDIS_HOST, REDIS_PORT (6380 for SSL), REDIS_PASSWORD, then run `python python/example.py` after `pip install -r python/requirements.txt`.