# Azure Cosmos DB (Mongo API) Sample

Node.js example using the MongoDB API. Set COSMOS_MONGO_URI, COSMOS_MONGO_USERNAME, COSMOS_MONGO_PASSWORD or edit defaults in `node/index.js`.

Quick start:
- cd node
- npm install
- npm start
