# Azure Database for PostgreSQL Samples

- `django/settings.snippet.py`: Django `DATABASES` config with SSL.
- `python/db_test.py`: Simple psycopg2 connectivity test. Install deps with `pip install -r python/requirements.txt`.