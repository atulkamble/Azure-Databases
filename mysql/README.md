# Azure Database for MySQL – WordPress Config Sample

See `wordpress/wp-config.sample.php` for a basic configuration. Replace values and rename to `wp-config.php` in your WordPress root.

Security tips:
- Prefer Private Endpoint or VNet Integration for app connectivity
- Enforce SSL and rotate credentials regularly